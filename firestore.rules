rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permit read and write access for all developers during testing and app preview
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
